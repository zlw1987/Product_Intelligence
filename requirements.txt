# Product Intelligence
#
# Intentionally minimal. Dependencies are added by the phase that needs them,
# not in advance. See docs/PRODUCT_INTELLIGENCE_PLAN.md for what is deferred.

# The floor is a supported release, not merely the oldest one whose API happens
# to compile. 5.1 has reached end of security support; 5.2 is the current LTS
# line and supports the interpreter this project develops on. The upper bound
# stays below 6.0 — moving to a new major line is its own decision, not a side
# effect of a dependency correction.
Django>=5.2,<6.0

# Production WSGI server for Windows-compatible deployment (PILOT-RELEASE-1).
# Gunicorn is Unix-only; Waitress is the approved Windows-compatible server.
# Waitress 3.0.2 verified on Python 3.14.7.
waitress>=3.0.2,<4.0

# PDF table extraction for specification enrichment (6D execution layer only).
# pdfplumber depends on pdfminer.six (transitive). Production imports both:
#   - pdfplumber (open PDFs, extract tables)
#   - pdfminer.six exception classes (bounded parser exception taxonomy)
# Neither may appear in research/ layer imports.
pdfplumber>=0.11.0
pdfminer.six>=20260107

# Development / test
pytest>=8.0
